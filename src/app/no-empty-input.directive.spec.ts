import { NoEmptyInputDirective } from './no-empty-input.directive';

describe('NoEmptyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEmptyInputDirective();
    expect(directive).toBeTruthy();
  });
});
