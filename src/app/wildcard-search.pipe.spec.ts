import { WildcardSearchPipe } from './wildcard-search.pipe';

describe('WildcardSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new WildcardSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
